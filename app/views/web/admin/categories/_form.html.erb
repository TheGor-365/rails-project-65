<%= form_with model: category, url: [:admin, category], local: true do |f| %>
  <div class="mb-3">
    <%= f.label :name, "Название", class: "form-label" %>
    <%= f.text_field :name, class: "form-control" %>
  </div>

  <%= f.submit class: "btn btn-primary" %>
<% end %>
